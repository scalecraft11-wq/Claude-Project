/**
 * Loading-screen timing constants — ANIMATION_BLUEPRINT.md §2. Kept
 * separate from `lib/animation/tokens.ts` because these govern a specific
 * product moment (the branded loader), not general-purpose motion timing.
 */
export const LOADER_MINIMUM_DURATION_MS = 500;
export const LOADER_MINIMUM_DURATION_MS_LUMORA = 600; // includes the brand-mark morph
